name: Security

on:
  pull_request:
    branches: [main]

permissions:
  actions: read
  contents: read
  security-events: write

jobs:
  # Fail only on vulnerabilities the pull request introduces (scans the npm
  # lockfile for the Prettier tooling and the pinned GitHub Actions).
  osv-scan:
    uses: google/osv-scanner-action/.github/workflows/osv-scanner-reusable-pr.yml@v2.5.1
